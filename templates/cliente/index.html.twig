{% extends 'base.html.twig' %} {% block title %}Hello ClienteController! {%
endblock %} {% block body %}
<style>
  .example-wrapper {
    margin: 1em auto;
    max-width: 800px;
    width: 95%;
    font: 18px / 1.5 sans-serif;
  }
  .example-wrapper code {
    background: #f5f5f5;
    padding: 2px 6px;
  }
</style>

<div class="example-wrapper">
  <h1>Hello! ✅</h1>

  This friendly message is coming from:
  <ul>
    <li>
      Your controller at
      <code>/var/www/guestbook/src/Controller/ClienteController.php</code>
    </li>
    <li>
      Your template at
      <code>/var/www/guestbook/templates/cliente/index.html.twig</code>
    </li>
  </ul>
</div>
{{ form(clientForm) }}
{# {{ form_start(clientForm) }}
{{ form_row(clientForm.fechaNacimiento, { attr: { class: "js-datepicker" } }) }}
{{ form_end(clientForm) }} #} {% endblock %} {# {% block javascripts %}
{{ parent() }}
<script>
  $(".js-datepicker").datepicker({
    format: "yyyy.MM.dd",
  });
</script>
{% endblock %} #}
